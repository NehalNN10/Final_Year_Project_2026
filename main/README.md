To be able to run the web application locally, follow these steps:
1. **Install the required libraries**  
   Open your terminal and run the following command to clone the repository:
   ```bash
   pip install -r requirements.txt
   ```
2. **Run the application**
    In the terminal, navigate to the directory where the application code is located and run:
    ```bash
    python app.py
    ```